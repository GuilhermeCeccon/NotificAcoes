import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
		height: 40,
        backgroundColor: "#fff",
		width: '85%',
		marginTop: '4%',
        borderRadius: 6,
        paddingLeft: 18
	}
});